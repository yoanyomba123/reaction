import colors from "../../../../config/colors"

const navHeight = 60

const textColor = "#fff"

const styles = {
  cont: {
    paddingTop: 30,
  },
  contBtn: {
    padding: 20,
    borderRadius: 5,
    borderWidth: 1,
    borderColor: "#eee",
    borderStyle: "solid",
  },
  textBtn: {
    fontSize: 25,
    color: "#000",
    textDecoration: "none",
  },
  textBtnMsg: {
    fontSize: 18,
    color: "#000",
    textDecoration: "none",
  }

}

export default styles
